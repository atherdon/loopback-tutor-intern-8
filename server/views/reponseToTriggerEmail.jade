button(style="background:none, border:none, padding:0, font: inherit, border-bottom:1px solid #000000, cursor: pointer")

h1 #{title}
if (typeof content !== 'undefined')
  p #{content}


form (action="#{redirectToEmail}" method='post')
  button (type='submit' value="#{redirectToLinkText}") #{redirectToLinkText}
   p for new email to complete registration.
   p Click
   a (href="#{redirectTo}")home
   p to try login again or sign up.
</form>